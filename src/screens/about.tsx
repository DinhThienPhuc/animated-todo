import { Box, Text, VStack } from 'native-base';

const AboutScreen = () => {
  return (
    <VStack flex={1}>
      <Box>
        <Text>About</Text>
      </Box>
    </VStack>
  );
};

export default AboutScreen;
